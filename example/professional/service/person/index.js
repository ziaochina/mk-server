const config = require('./config')
const api = require('./api')

module.exports = {
    name: "person",
	version: "",
    description: "",
    author: "lsg",
    config,
    api,
    dependencies:['utils'],
}