const config = require('./config')
const api = require('./api')

module.exports = {
    name: "user_log",
	version: "",
    description: "",
    author: "lsg",
    config,
    api,
    dependencies:['utils'],
}